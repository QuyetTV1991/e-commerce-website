import React from "react";
import Button from "../button/Button";

const CartCheckout = props => {
  return <Button onClick={props.onClick}>checkout process</Button>;
};

export default CartCheckout;
